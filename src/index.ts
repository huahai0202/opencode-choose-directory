import { Plugin } from "@opencode-ai/plugin"

export default Plugin.define({
  id: "opencode-choose-directory",
  tui: true,
  setup() {
    // All functionality is implemented by the TUI entrypoint.
  },
})
